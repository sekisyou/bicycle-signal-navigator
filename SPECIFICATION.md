# Nav 仕様書

この文書は、`/Users/kawase/nav` の現行コードを元にした仕様書です。実装に合わせて書いているため、理想仕様ではなく「今のプログラムが何をしているか」を説明します。

## 1. アプリの目的

このアプリは、自転車や徒歩に近い移動を想定したルート作成・ナビ・信号観測アプリです。主な目的は以下です。

- ルートを作成して保存する
- ルート上の信号を登録する
- 走行中またはシミュレーション中に次の信号を案内する
- 信号で赤停止・青通過を観測として保存する
- 過去観測から信号周期を推定し、次の青に合わせた推奨速度帯を出す

## 2. 全体構成

アプリは大きく 4 層に分かれています。

- UI層
  - `src/App.jsx`
  - `src/ui/MapPage.jsx`
  - `src/ui/NavPage.jsx`
  - `src/ui/NavRouteMode.jsx`
  - `src/ui/NavSignalMode.jsx`
  - `src/ui/SignalPanel.jsx`
  - `src/ui/AssistSpeedSlot.jsx`
- データ保存層
  - `src/firebase.js`
  - `src/routesStore.js`
  - `src/observationStore.js`
- 推定・制御ロジック層
  - `src/core/inferTG.js`
  - `src/core/speedAssist.js`
  - `src/core/godSignal.js`
  - `src/core/signalApproachDistanceOnly.js`
  - `src/core/math.js`
- 地図・ルート生成層
  - `react-leaflet`
  - Mapbox Directions API

## 3. 画面仕様

### 3.1 ログイン画面

未ログイン時はログイン画面を表示します。

- メールアドレス + パスワードでログイン可能
- メールアドレス + パスワードで新規登録可能
- Google ログイン可能
- Firebase Authentication を使用

実装上は `App.jsx` で `onAuthStateChanged` を監視し、ユーザーが存在しない場合にログインUIを表示します。

### 3.2 Map 画面

`MapPage` はルート作成・編集画面です。

機能は以下です。

- 保存済みルート一覧表示
- 新規ルート作成
- 既存ルート編集
- ルート削除
- 出発地点と到着地点の設定
- 通過地点の追加
- 信号地点の追加
- ルートの自動生成
- Firestore への保存

#### ステージ

ルート作成は 3 ステージです。

- ステージ1: 出発/到着
- ステージ2: 通過地点
- ステージ3: 信号

#### ルート生成

出発地点と到着地点が揃うと、Mapbox Directions API でルートを生成します。通過地点が追加された場合も再生成します。

#### 信号追加

信号はルートから 30m 以内でのみ追加可能です。追加時にルート上の最寄り点へスナップされます。

### 3.3 Nav 画面

`NavPage` はナビゲーション画面です。

機能は以下です。

- 現在地追従
- 次信号または目的地の表示
- 速度推定
- 信号予測に基づく推奨速度帯表示
- ルートモードと信号モードの切り替え
- シミュレーション機能
- 神信号モード

### 3.4 Signal 画面

`NavSignalMode` と `SignalPanel` は、信号接近時の専用画面です。

ここで以下を行います。

- 赤の手動入力
- 青の手動入力
- 赤の自動検出
- 通過の自動検出
- 赤後青の自動検出
- 信号モードからルートモードへの自動復帰

## 4. データモデル

### 4.1 routes コレクション

保存先:

- `users/{uid}/routes/{routeId}`

主な項目:

- `name`: ルート名
- `useTimeSecOfDay`: 使う出発時刻。秒単位
- `origin`: 出発地点
- `dest`: 到着地点
- `viaPts`: 通過地点配列
- `signals`: 信号配列
- `routePts`: ルートポリライン
- `distM`: ルート全長
- `createdAt`
- `updatedAt`

### 4.2 signal オブジェクト

各信号はルート内の `signals[]` に保存されます。

主な項目:

- `id`
- `name`
- `lat`
- `lng`
- `needsFix`
- `distance`: ルート上の距離 `s`
- `obsCount`: この信号での観測回数
- `cycle`: 将来拡張用の周期情報格納欄

現状では予測結果は `signals[].cycle` に毎回保存しているわけではなく、必要時に観測データから再推定しています。

### 4.3 observations コレクション

保存先:

- `users/{uid}/routes/{routeId}/observations/{obsId}`

観測は 2 種類あります。

#### blue 観測

青のまま通過した観測です。

- `signalId`
- `color: "blue"`
- `day`: JST 日インデックス
- `t`: 通過時刻 epoch ms

#### red 観測

赤停止して、その後青で発進した観測です。

- `signalId`
- `color: "red"`
- `day`: JST 日インデックス
- `t`: 赤到着時刻 epoch ms
- `nextGreen`: 青開始時刻 epoch ms
- `wait`: 待ち時間 ms

ここで観測の本質は `t` と `nextGreen` です。`wait` は `nextGreen - t` の派生値です。

## 5. 時刻系の仕様

### 5.1 day の意味

`day` は JST(+09:00) の日付区切りで計算した日インデックスです。

### 5.2 t の意味

保存上の `t` は epoch milliseconds です。

### 5.3 inferTG に渡す時刻

`routesStore.getObsForInferSignalUnified` は、Firestore から取得した観測を `inferTG` 用に秒へ変換して返します。

- Firestore 保存時: ms
- 推定時: sec

この変換は `t`, `wait`, `nextGreen` に対して行われます。

## 6. ルート作成仕様

### 6.1 出発/到着設定

Map 画面で地図クリックまたは現在地取得により設定します。

### 6.2 通過地点

通過地点はステージ2で追加します。ルート生成時に Mapbox の経由点として使います。

### 6.3 信号地点

信号はステージ3で追加します。

- ルートに十分近い場所でのみ追加できる
- 追加時にルート上へスナップ
- 保存時も再スナップされる
- ルートから 30m 以上離れている信号は保存時に除外される

### 6.4 保存時の検証

保存前に以下を検証します。

- ログインしていること
- ルート名があること
- 出発/到着があること
- ルートポリラインがあること
- 出発時刻形式が有効なこと
- 同名ルートが存在しないこと

## 7. ナビ画面仕様

### 7.1 現在地

現在地は次のいずれかです。

- 実GPS
- シミュレーション位置

`simEnabled` がオンならシミュレーション位置、オフなら実GPSを使用します。

### 7.2 速度

速度は以下の 3 種類を使います。

- `displaySpeedKmh`
  - 表示用平均速度
  - 直近 500ms のサンプル平均
- `judgeSpeedKmh`
  - 通過判定用平均速度
  - 直近 1000ms の平均
- `redJudgeSpeedKmh`
  - 赤自動検出用平均速度
  - 直近 2000ms の平均

サンプル保持期間は 4 秒です。

### 7.3 次ターゲット

`NavRouteMode` は次の表示ターゲットを決めます。

優先順位:

1. 未通過の信号
2. 目的地

ただし内部の reroute 用目標としては、表示信号より手前に未通過の通過地点があれば、その通過地点を優先して再ルートします。

### 7.4 信号モード突入

表示対象の次信号に対し、直線距離が 30m 以下になると `onEnterSignalMode` を呼び、信号モードに入ります。

## 8. 予測仕様

### 8.1 予測の目的

予測は、対象信号について以下を推定するために使います。

- `T`: 周期長
- `G`: 青時間
- `R`: 赤時間
- `theta`: 当日の青開始位相
- `beta1`: 日ごとの位相ずれ

この結果を使って、今の速度と信号までの距離に対して「どの速度帯なら青で入れるか」を提示します。

### 8.2 予測に使う観測

観測は対象信号単位で取得します。

- 赤観測を最大 30 件
- 青観測を最大 10 件

古い観測も使いますが、重みは新しいものほど大きくなります。重みは日数に対する指数減衰です。

### 8.3 inferTG の入力解釈

`inferTG` は各観測を次の形に正規化します。

- `arrivalObs`
  - 信号へ到達した観測時刻
  - `t`
- `departObs`
  - 赤観測なら青に変わった時刻
  - `nextGreen` もしくは `t + wait`
- `colorObs`
  - `red` または `blue`

### 8.4 推定モデル

モデルは「日によって青開始位相が少しずれる周期信号」です。

当日の青開始絶対時刻はおおむね次で表現されます。

- `theta(day) = beta0 + beta1 * (day - anchorDay)`

ここで:

- `beta0`: 基準日の位相
- `beta1`: 1日あたりの位相ずれ
- `T`: 周期

### 8.5 Step1: T と beta1 の探索

`inferTG` は `T_MIN` から `T_MAX` の範囲で周期候補を全探索し、各 `T` に対して `beta1` も探索します。

現行設定:

- `T_MIN = 30`
- `T_MAX = 200`
- `BETA_STEP = 1` または既定値では `0.1`

評価に使う要素:

- 赤観測の `departObs` がモデルの青開始位相と整合すること
- 必要に応じて、青観測が青区間に、赤観測が赤区間に入るような位相分離ペナルティ

最もスコアの良い `T` と `beta1` を採用します。

### 8.6 anchor 推定

初期 anchor は「最新の red 観測の departObs」です。

その後、複数の red 観測の departObs を使って `beta0` を再推定します。

方法:

1. 各 red 観測について `Xi = departObs - beta1 * (day - anchorDay)` を計算
2. `Xi mod T` を円統計で平均
3. 最新 red 観測に最も近い整数周期へ持ち上げて絶対 `beta0` を決定

この再推定後にもう一度 `T` と `beta1` を推定し直します。

### 8.7 Step2: G と R の推定

`T` と `beta1` が決まった後、各観測の到達位相 `phi` を計算します。

そこから:

- blue 観測群の上端から `Ghat` を推定
- red 観測群から `Rhat` を推定し、`Gred = T - Rhat` を計算

最後に状況に応じて以下を使います。

- `Gsimple`
- `Ghat`
- `Gred`
- `Ghat` と `Gred` の融合値

### 8.8 confidence

信頼度は簡易指標で、主に赤観測数に応じて上がります。

現行では:

- 赤観測 0 件に近いと 0.5 付近
- 赤観測 10 件以上で 1.0 に近づく

### 8.9 予測結果の使い道

予測結果は `NavPage` で `buildSpeedRanges` に渡されます。

必要な値:

- 信号まで距離
- 現在速度
- `T`
- `G`
- `thetaAbs`
- 現在の sec-of-day

## 9. 推奨速度帯仕様

### 9.1 目的

推奨速度帯は、「今からその速度で進んだとき、次の青開始または青継続中に信号へ到達できる速度範囲」です。

### 9.2 使う候補

`speedAssist` は現在速度から到達予想時刻を出し、その時刻が属する青開始タイミングの候補を 3 つ作ります。

- `top`: 1周期前
- `mid`: 現在周期
- `bot`: 1周期後

ただし赤到達になる場合は `top` を無効化します。

### 9.3 範囲計算

各候補について:

- 遅い側: 青終了ギリギリに到達する速度
- 速い側: 青開始ギリギリに到達する速度

これを km/h に変換して速度帯にします。

### 9.4 UIへの表示

速度帯は `AssistSpeedSlot` に渡され、現在速度との位置関係を表示します。

## 10. 信号モード仕様

### 10.1 目的

信号モードは、特定信号への接近から通過完了までを状態機械として扱い、観測を保存するための画面です。

### 10.2 入力値

主な入力:

- 現在GPS位置
- 対象信号
- 進入時の速度
- 現在の表示速度 / 判定速度 / 赤判定速度
- 現在時刻
- 神信号設定
- 通常 / 渋滞モード

### 10.3 速度帯

信号モードでは速度を 3 帯に分けます。

- `slow`
- `mid`
- `fast`

ヒステリシス付きで判定します。

### 10.4 赤の自動検出

通常モードかつ神信号オフ時のみ有効です。

条件:

- まだ red 測定していない
- post-red-blue 測定していない
- 通過確定していない
- 信号まで 12m 以内
- `redJudgeSpeedKmh < 2.0`

満たした場合、現在時刻を赤到着時刻として `redMeasuredAt` に記録します。

### 10.5 通過の自動検出

通常モードかつ神信号オフ時のみ有効です。

条件:

- red 未測定
- blueCandidate 未設定
- 低速帯ではない
- 最短接近時刻がある
- 現在のルート距離 `sCurrent` が、最短接近時の `s` より一定以上進んでいる
- 信号への直線距離が、最短距離より一定以上増えている

速度帯ごとのしきい値:

- `mid`: 前進 3m 以上、距離増加 3m 以上
- `fast`: 前進 4m 以上、距離増加 4m 以上

成立したら:

- `blueCandidateAt = minDistanceAt`
- `passDetectedAt = currentNowMs`
- `lockedSpeedBand = speedBand`

を設定します。

### 10.6 赤後青の自動検出

red 測定後に青で再発進したとみなす条件です。

条件:

- red 測定済み
- まだ postRedBlueAt がない
- red 検出時の `s` がある
- 現在 `s` が red 検出位置より 3m 以上進んでいる
- `judgeSpeedKmh >= 4.0`

成立したら `postRedBlueAt` を設定します。

### 10.7 自動復帰

青通過後は速度帯に応じて信号モードを自動終了します。

- fast: 2 秒後
- mid: 4 秒後
- slow 手動青: 即時タイマーで復帰

さらに低速イベントが起きると 4 秒ずつ延長します。

### 10.8 強制復帰

red 測定なしで近接済み、かつその後 15m 以上離れたら強制的に route モードへ戻ります。

これは通過したのに通常復帰条件を取り逃した場合の保険です。

### 10.9 渋滞モード

渋滞モードでは通常の自動判定を抑制し、手動入力中心の運用を想定しています。

### 10.10 神信号モード

神信号は観測ではなく、理想的な信号モデルを外部設定値で与えるモードです。

入力:

- `T`
- `G`
- `beta0`
- `beta1`
- `day`
- `nowSec`
- `anchorDay`

`godSignalState` は現在位相を計算し、今が青か赤か、次に赤/青まで何秒かを返します。

信号モード側では:

- 停止線到達時に青ならそのまま通過扱い
- 停止線到達時に赤なら強制停止
- その後青になったら再発進

という制御を行います。

## 11. 観測保存仕様

信号モード終了時、`handlePassSignal` が観測を Firestore に保存します。

### 11.1 青通過のみ

条件:

- `redAt` がない
- `blueAt` がある

保存:

- `saveBlueObservation`

### 11.2 赤停止あり

条件:

- `redAt` がある

保存:

1. `startRedObservation` で red 観測を作成
2. `postRedBlueAt` があれば `finishRedObservation` で red 観測を完了

### 11.3 obsCount

`startRedObservation` または `saveBlueObservation` の時点で、その信号の `obsCount` を 1 増やします。

## 12. ルートモード自動処理

### 12.1 通過地点の自動通過

現在位置が通過地点から 10m 以内に入ると、その通過地点を `passedViaIds` に追加します。

### 12.2 次信号の固定

一度選ばれた信号は、スキップまたは通過済みになるまで固定されます。

### 12.3 reroute

現在位置がルートから 10m 超外れ、その状態が 5 秒続くと再ルートを試みます。

再ルート先は:

- 表示信号より手前の未通過通過地点があればそこ
- なければ次信号または目的地

Mapbox で現在位置から目標までの区間を生成し、既存ルートの後半に接続します。

## 13. シミュレーション仕様

`NavPage` にはシミュレーション機能があります。

主な機能:

- シミュレーション ON/OFF
- ドラッグモード
- 自動走行モード
- 速度設定
- 一時停止
- 開始時刻の再抽選

シミュレーション時刻は `simNowMs` として保持され、実GPSの代わりに `simPos` を使います。

出発時刻にはランダムゆらぎを入れられます。

## 14. 使っていない / 部分的な実装

現行コード上、以下は「将来拡張用」または「部分的」です。

- `signals[].cycle`
  - 構造はあるが、常時同期保存していない
- `signalApproachDistanceOnly`
  - 汎用の距離のみ通過判定ロジック。現行 UI の主経路では中心ではない
- `SimPage`
  - `App.jsx` に分岐はあるが、Map -> Nav の遷移が中心

## 15. 制約と注意点

- 予測は観測数が少ないと不安定になりうる
- 推定は対象信号単位で行われ、ルート全体一括学習ではない
- Firestore 保存は ms、推定時は sec なので単位変換が前提
- UI 表示用の速度と判定用速度は別
- 自動検出は通常モード前提で、渋滞モードでは抑制される
- 地図クリックやドラッグが多いので、誤判定抑制のために一時ブロック処理がある

## 16. 実装上の中心フロー

通常の利用フローは次の通りです。

1. Map 画面でルートを作る
2. 信号をルート上に配置して保存する
3. Nav 画面で移動する
4. 次信号の観測履歴を取得する
5. `inferTG` で周期を推定する
6. `buildSpeedRanges` で推奨速度帯を計算する
7. 信号 30m 手前で信号モードへ入る
8. 赤/青の自動検出または手動入力を行う
9. 終了時に観測を保存する
10. 観測が蓄積され、次回以降の予測精度が上がる

## 17. 補足: この仕様書の位置づけ

この文書は「コードから逆算した仕様書」です。つまり:

- 実装されているものは書く
- 実装されていない理想仕様は書かない
- 一部の挙動はコード上のしきい値に従う

仕様を変更したい場合は、この文書を元に

- 保存仕様の見直し
- 自動検出条件の見直し
- 推定アルゴリズムの見直し
- UIフローの見直し

を個別に進めるのが良いです。
